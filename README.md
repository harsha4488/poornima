# Poornima Jewellers React + Vite

Use this in the existing `harsha4488/poornima` repository.

## Local
npm install
npm run dev

## Production
npm run build

GitHub Pages is already configured with `public/CNAME` for:
poornimajewellers.com
